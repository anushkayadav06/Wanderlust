<style>
    .btn-box {
        border-radius: 2rem;
        padding: 0.5rem 3rem 0.5rem;
    }

    .btn1-box {
        border-radius: 2rem;

    }

    .btn {
        background-color: #fe424d;
        color: white;
        border-color: #fe424d;
    }

    .btn:hover {
        background-color: #fe424d;
        color: white;
        border-color: #fe424d;
    }

    .search-btn {
        display: inline;
        margin-right: 0.5rem;
    }

    @media (max-width: 768px) {
    .container-fluid {
        background-color: white !important;
        box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);

    }
    .btn-box{
        width: 50%;
    }
}
</style>
<nav class="navbar navbar-expand-md border-bottom sticky-top bg-md-white">
    <div class="container-fluid ">
        <a class="navbar-brand" href="/listings"><i class="fa-solid fa-compass"></i></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
                <a class="nav-link" href="/listings">Explore</a>
            </div>
            <div class="navbar-nav ms-auto">
                <form action="/listings/search" method="GET" class="d-flex " role="search">
                    <input class="form-control me-2 btn-box" type="search" name="q" placeholder="Search destination" />
                    <button class="btn  btn1-box" type="submit">
                        <i class="fa-solid fa-magnifying-glass search-btn"></i>Search</button>
                </form>
            </div>

            <div class="navbar-nav ms-auto">
                <a class="nav-link" href="/listings/new">Airbnb your home</a>
                <% if(!currUser) { %>
                    <a class="nav-link " href="/signup"><b>Sign In</b></a>
                    <a class="nav-link" href="/login"><b>Login</b></a>
                    <% } %>
                        <% if(currUser) { %>
                            <a class="nav-link" href="/logout">Logout</a>
                            <% } %>

            </div>
        </div>
    </div>
</nav>